# Example .env for `be/build_and_push.sh`
# Copy this to `be/.env` and fill in values. Do NOT commit `be/.env`.

# Docker image name (required)
IMAGE=yourusername/yourrepo

# Optional explicit overrides (if you want separate names)
# BACKEND_IMAGE=yourusername/backend:tag
# FRONTEND_IMAGE=yourusername/frontend:tag

# Optional: local convenience (prefer to use GitHub Secrets for CI)
# DOCKERHUB_USERNAME=yourusername
# DOCKERHUB_TOKEN=your_token_here
