# Example .env for the project root. Copy this file to `.env` at the project root
# and edit the values (do NOT commit the resulting `.env`). This single `.env` is used
# by all helper scripts (build_and_push, pull_and_run, docker compose flows).

# Docker image base name (optional)
# If set, the root build script will derive backend and frontend image names from this base.
IMAGE=yourusername/yourrepo

# Optional explicit overrides
# BACKEND_IMAGE=yourusername/backend:tag
# FRONTEND_IMAGE=yourusername/frontend:tag

# Optional deployment settings
# BACKEND_PORT=8000
# FRONTEND_PORT=5173
# FRONTEND_URL=https://yourdomain.com

# Example runtime vars for frontend preview / production
# ALLOWED_HOSTS=yourdomain.com,localhost
# HOST=0.0.0.0

# Optional: local convenience (prefer to use CI secrets in production)
# DOCKERHUB_USERNAME=yourusername
# DOCKERHUB_TOKEN=your_token_here
